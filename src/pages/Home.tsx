import React, { FC } from 'react';
import { Button } from 'antd';

const Home: FC = () => {
  return (
    <div>
      <p>Home</p>
      <Button> antd button </Button>
    </div>
  );
};

export default Home;
